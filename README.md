# WebRTC Video Chat


## Requirements
* Node.js 8+
* MongoDB 4

## Instalation
* create database 'chat' in mongodb
* run `npm install` in project root folder

## Usage
run `node bin/www`
